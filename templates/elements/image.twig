<img
        src="{{ Href }}"
        alt="{{ Description }}"
        style="
                {%  if Float %}float: {{ Float }};{% endif %}
                {%  if Width %}width: {{ Width }};{% endif %}
                {%  if Height %}height: {{ Height }};{% endif %}
        "
/>